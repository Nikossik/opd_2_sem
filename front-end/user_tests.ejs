<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="styles.css">
    <title>Тесты пользователя <%= user.login %></title>
</head>
<body>
    <h1>Тесты пользователя <%= user.login %></h1>


    <table>
        <thead>
            <tr>
                <th>Тип теста</th>
                <th>Количество</th>
                <th>Среднее</th>
                <th>Дисперсия</th>
                <th>Отклонение</th>
                <th>Медиана</th>
                <th>Мода</th>
            </tr>
        </thead>
        <tbody>
            <% if (testResults && testResults.length > 0) { %>
                <% testResults.forEach(result => { %>
                    <tr>
                        <td><%= result.type %></td>
                        <td><%= result.count %></td>
                        <td><%= result.mean.toFixed(2) %></td>
                        <td><%= result.variance.toFixed(2) %></td>
                        <td><%= result.stdDeviation.toFixed(2) %></td>
                        <td><%= result.median.toFixed(2) %></td>
                        <td><%= result.mode %></td>
                    </tr>
                <% }) %>
            <% } else { %>
                <tr>
                    <td colspan="7">Нет данных для отображения.</td>
                </tr>
            <% } %>
        </tbody>
    </table>
    <form action="/delete_user/<%= user.id %>" method="POST" onsubmit="return confirm('Вы уверены, что хотите удалить этого пользователя?')">
            <button type="submit">Удалить пользователя</button>
        </form>
    <button onclick="location.href='/all_users'">Назад</button>
</body>
</html>
