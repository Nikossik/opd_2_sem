<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Список Экспертов</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="AdminPageStyle.css">
    <style>
        body {
        background-image: url('images/background_gradient.png');
        background-size: cover; 
        background-position: center; 
        }
    </style>
</head>
<body>
    <div class="menu-wrap">
        <input type="checkbox" class="toggler">
        <div class="main-menu-icon"><div></div></div>
        <div class="menu">
            <div>
                <div>
                    <ul>
                        <li><a href="/">ДОМАШНЯЯ СТРАНИЦА</a></li>
                        <!-- <li><a href="#">О САЙТЕ</a></li> -->
                        <li>
                            <% if (!loggedIn) { %>
                                <a href="/login">ВОЙТИ</a>
                            <% } %>
                        </li>
                        <li><a href="/characteristics">ПРОФЕССИИ</a></li>
                        <!-- <li><a href="/adminPage">АДМИНИСТРАТОРЫ</a></li> -->
                        <li>
                            <% if (adminUser) { %>
                                <a href="/poll_1_part_1">ОПРОС</a>
                            <% } %>
                        </li>
                        <li><% if (adminUser) { %>
                            <a href="/polls_results">РЕЗУЛЬТАТЫ ТЕСТИРОВАНИЯ</a>
                        <% } %>
                        </li>
                        <li><% if (adminUser) { %>
                                <a href="/adminRegister">ДОБАВИТЬ АДМИНА</a>
                            <% } %>
<!--                        </li>-->
<!--                        <li><% if (adminUser) { %>-->
<!--                            <a href="/create_invite">ССЫЛКА-ПРИГЛАШЕНИЕ</a>-->
<!--                        <% } %>-->
<!--                        </li>-->
                        <li>
                            <% if (loggedIn) { %>
                                <a href="/adminPage">ЭКСПЕРТЫ</a>
                            <% } %>
                        </li>
                        <li>
                            <% if (loggedIn) { %>
                                <a href="/logout">ВЫЙТИ</a>
                            <% } %>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>

    <section class="container">
        <h1 class="admin_text">Эксперты</h1>
        <div class="admin-container">
            <% logins.forEach(function(admin) { %>
                <button class="admin-button" data-id="<%= admin.id %>">
                    <%= admin.login %> <br>(ID: <%= admin.id %>)
                </button>
            <% }); %>
        </div>
    </section>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const buttons = document.querySelectorAll('.admin-button');
            buttons.forEach(button => {
                button.addEventListener('click', function() {
                    const id = this.getAttribute('data-id');
                    window.location.href = '/expert_' + id; 
                });
            });
        });
    </script>
    


</body>
</html>

